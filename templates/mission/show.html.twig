{% extends 'base.html.twig' %}

{% block title %}Mission{% endblock %}

{% block body %}
    <h1>Mission</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdMission</th>
                <td>{{ mission.idMission }}</td>
            </tr>
            <tr>
                <th>Matricule</th>
                <td>{{ mission.matricule }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ mission.description }}</td>
            </tr>
            <tr>
                <th>HeureDebut</th>
                   <td>{{ mission.heureDebut|date('d-m-Y H:i') }}</td>
                       
            </tr>
            <tr>
                <th>HeureFin</th>
                 <td>{{ mission.heureFin|date('d-m-Y H:i')  }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_mission_index') }}"class="btn btn-primary btn-custom " style="width:200px;  white-space: nowrap; "><i class="fas fa-arrow-left"></i>    Liste des Missions</a>
        <a href="{{ path('app_home_admin') }}"class="btn btn-primary btn-custom" style="width:200px;  white-space: nowrap; float: right; "><i class="fas fa-arrow-right"></i> Dashboard</a>

   {# <a href="{{ path('app_mission_edit', {'idMission': mission.idMission}) }}">edit</a>

    {{ include('mission/_delete_form.html.twig') }} #}
{% endblock %}
