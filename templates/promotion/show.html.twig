{% extends 'base.html.twig' %}

{% block title %}Promotion{% endblock %}

{% block body %}
    <h1>Promotion</h1>

    <table class="table">
        <tbody>

            <tr>
                <th>NomP</th>
                <td>{{ promotion.nomP }}</td>
            </tr>
            <tr>
                <th>DureeP</th>
                <td>{{ promotion.DureeP }}</td>
            </tr>
            <tr>
                <th>PrixAvant</th>
                <td>{{ promotion.PrixAvant }}</td>
            </tr>
            <tr>
                <th>PrixApres</th>
                <td>{{ promotion.PrixApres }}</td>
            </tr>
            <tr>
                <th>Pourcentage</th>
                <td>{{ promotion.Pourcentage }}</td>
            </tr>
           <tr>
                <th>Offre Description</th>
                <td>{{ promotion.offre.description }}</td>
            </tr>
            <tr>
                <th>Offre Duree</th>
                <td>{{ promotion.offre.dureeO }}</td>
            </tr>

        </tbody>
    </table>

   <a href="{{ path('app_promotion_index') }}" class="btn btn-primary float-right">
    <i class="fas fa-arrow-left
    "></i> Back to list
</a>

    <a href="{{ path('app_promotion_edit', {'id': promotion.id}) }}">edit</a>

    {{ include('promotion/_delete_form.html.twig') }}
{% endblock %}
