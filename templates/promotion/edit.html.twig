{% extends 'base.html.twig' %}

{% block title %}Edit Promotion{% endblock %}

{% block body %}
{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    <h1>Edit Promotion</h1>

    {{ include('promotion/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_promotion_index') }}">back to list</a>

    {{ include('promotion/_delete_form.html.twig') }}
    {{ form_errors(form.nomP)}}
    {{ form_errors(form.DureeP)}}
    {{ form_errors(form.PrixAvant)}}
    {{ form_errors(form.PrixApres)}}
    {{ form_errors(form.Pourcentage)}}
    {{ form_end(form) }}
{% endblock %}
