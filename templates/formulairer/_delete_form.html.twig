<form method="post" action="{{ path('app_formulairer_delete', {'id': formulairer.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ formulairer.id) }}">
    <button class="btn">Delete</button>
</form>
